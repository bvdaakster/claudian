# Claudian SSH configuration
# Allow root login with password (change to 'prohibit-password' for key-only auth)
PermitRootLogin yes

# For key-based authentication only, use:
# PermitRootLogin prohibit-password
# Then place your public key in /root/.ssh/authorized_keys
